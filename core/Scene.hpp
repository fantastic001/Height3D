
#ifndef HEIGHT3D_SCENE_HPP
#define HEIGHT3D_SCENE_HPP

class Scene 
{

public:
	Scene(float a, float b, float c);

	// addObject(AbstractModel model, float x, float y, float z, float alpha, float phi, float a, float b, float c);
	// addLight(AbstractLight, float x, float y, float z);
	// setCamera(float x, float y, float z, float lx, float ly, float lz);
	// getModelMatrix(float x, float y, float z, float alpha, float phi, float a, float b, float c);
	// getPerspective(float x, float y, float z, float lx, float ly, float lz, float fovy);
	
	// getMatrix(
	//		float x, float y, float z, float alpha, float phi, float a, float b, float c,
	//		float cx, float cy, float cz, float lx, float ly, float lz, float fovy
	//		);

	// void drawObjects(int modelUniformMatrixLocation, int PerspectiveMatrixLocation);
};

#endif // HEIGHT3D_SCENE_HPP
